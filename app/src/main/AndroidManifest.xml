<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BypassPathTraversal"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".VulProvider1"
            android:authorities="slipme1"
            android:exported="true" />
        <provider
            android:name=".VulProvider2"
            android:authorities="slipme2"
            android:exported="true" />
        <provider
            android:name=".VulProvider3"
            android:authorities="slipme3"
            android:exported="true" />
        <provider
            android:name=".VulProvider4"
            android:authorities="slipme4"
            android:exported="true" />
        <provider
            android:name=".VulProvider5"
            android:authorities="slipme5"
            android:exported="true" />
    </application>

</manifest>